# Next.js 14 Rules

* Use App Router for all new routes.
* Maximize the use of Server Components.
* Only add `"use client"` when interactivity, hooks (`useState`, `useEffect`), or browser APIs are required.
* Keep client boundaries as low in the component tree as possible.
* Use Next.js Image component for optimized images.
* Preload critical assets where possible.
