# Upload Rules

* Only allow authenticated admins to upload files.
* Strictly validate file MIME types on the server side.
* Reject executable files.
* Limit file size to a reasonable maximum (e.g., 5MB for images).
