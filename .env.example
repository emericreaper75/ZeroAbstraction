# Site URL used for sitemap and OpenGraph
NEXT_PUBLIC_SITE_URL=https://zero-abstraction.dev

# Admin credentials used by scripts/seed-admin.ts (placeholders - do NOT commit real secrets)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMeToAStrongPassword

# Database and auth placeholders
DATABASE_URL=postgresql://postgres:password@localhost:5432/zeroabstraction
AUTH_SECRET=replace_with_random_value
NEXTAUTH_SECRET=replace_with_random_value
